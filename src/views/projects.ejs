<%- include('partials/header') %>

    <main>
        <h1>
            <%= title %>
        </h1>

        <p>
            Explore our various service categories to find opportunities that match your interests:
        </p>

        <ul>
            <% categories.forEach(category=> { %>
                <li>
                    <%= category.name %>
                </li>
                <% }) %>
        </ul>
    </main>

    <%- include('partials/footer') %>