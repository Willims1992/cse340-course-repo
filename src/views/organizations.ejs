<%- include('partials/header') %>

    <main>
        <h1>
            <%= title %>
        </h1>

        <p>Here are a few of our partner organizations:</p>

        <ul>
            <% if (organizations && organizations.length> 0) { %>
                <% organizations.forEach(organization=> { %>
                    <li>
                        <img src="/image/<%= organization.logo_filename %>" alt="<%= organization.name %> logo">
                        <strong>
                            <%= organization.name %>
                        </strong>:
                        <%= organization.contact_email %>
                    </li>
                    <% }) %>
                        <% } else { %>
                            <p>No organizations found.</p>
                            <% } %>
        </ul>
    </main>

    <%- include('partials/footer') %>



    <!-- iam  commenting out the coder below for learning and experimentation purposes -->

    <!--<% if (false) { %>
        <%- include('partials/header') %>
    
            <main>
                <h1>
                    <%= title %>
                </h1>
    
                <p>Here are a few of our partner organizations:</p>
                <ul>
    
                    <% organizations.forEach(organization=> { %>
                        <li>
                            <img src="/image/<%= organization.logo_filename %>" alt="<%= organization.name %> logo">
                            <strong>
                                <%= organization.name %>
                            </strong>:
                            <%= organization.contact_email %>
                        </li>
                        <% }); %>
    
                </ul>
            </main>
    
            <%- include('partials/footer') %>
                <% } %>-->